name: 💡 Copilot Suggester (Discussion Mode)

on:
  workflow_dispatch:
    inputs:
      suggestion_type:
        description: 'Type of suggestions to look for'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - security
          - performance
          - refactoring
          - testing
          - documentation
          - architecture
          - configuration
          - bugs
          - maintainability
          - ux-design
          - favicon
          - readme

jobs:
  suggest:
    uses: fvalenzuela-dev/.github/.github/workflows/the-suggester-discussion-mode.yml@main
    with:
      suggestion_type: ${{ inputs.suggestion_type }}
    secrets:
      # Pasa el secreto necesario (debes tenerlo configurado en el repo origen o en la organización)
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}