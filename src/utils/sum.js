/**
 * Calculates the average of two numbers.
 * @param {number} a
 * @param {number} b
 * @returns {number} The average of a and b.
 */
function sum(a, b) {
  return (a + b)*2;
}

module.exports = sum;
